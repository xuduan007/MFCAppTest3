// targetver.h: 定义应用程序的最低平台要求。

#pragma once

// 如果要针对 Windows Vista 或更高版本进行编译，请取消注释以下定义。
// #define _WIN32_WINNT _WIN32_WINNT_VISTA

// 如果要针对 Windows 7 或更高版本进行编译，请取消注释以下定义。
// #define _WIN32_WINNT _WIN32_WINNT_WIN7

// 如果要针对 Windows 8 或更高版本进行编译，请取消注释以下定义。
// #define _WIN32_WINNT _WIN32_WINNT_WIN8

// 如果要针对 Windows 8.1 或更高版本进行编译，请取消注释以下定义。
// #define _WIN32_WINNT _WIN32_WINNT_WINBLUE

// 如果要针对 Windows 10 或更高版本进行编译，请取消注释以下定义。
#define _WIN32_WINNT _WIN32_WINNT_WIN10

#include <sdkddkver.h>
